== README

A blog rendered entirely on an infinite canvas, with each post represented as a polaroid picture.

* Configuration
git clone
bundle install
rake db:create
rake db:migrate
rake db:seed
rails s

You can log using the email test@test.com, password: password to see an example account.